/**
 * @name __
 * @since  v0.1.0
 * @description Placeholder value, used to set placeholder in arguments
 * @type {Object}
 * @example
 * const test = (a, b) => return b;
 * const ex = test(__, 2); // => 2
 */
export default { '@@functional/placeholder': true }; // eslint-disable-line no-underscore-dangle